{
  "buildCommand": "mkdir -p public && cp index.html dashboard.html style.css config.json config.js script.js dashboard.js utils.js public/ && rm -rf public/api && cp -r api public/",
  "outputDirectory": "public",
  "cleanUrls": false
}
